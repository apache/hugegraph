<?xml version="1.0" encoding="UTF-8"?>
<!--
  Licensed to the Apache Software Foundation (ASF) under one or more
  contributor license agreements.  See the NOTICE file distributed with
  this work for additional information regarding copyright ownership.
  The ASF licenses this file to You under the Apache License, Version 2.0
  (the "License"); you may not use this file except in compliance with
  the License.  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
  -->

<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.apache.hugegraph</groupId>
        <artifactId>hugegraph-store</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>hg-store-cloud-s3</artifactId>

    <description>
        S3 cloud storage provider plugin for HugeGraph Store.
        Add this JAR to the classpath and set cloud.storage.provider=s3 to enable S3 offload.
    </description>

    <properties>
        <aws.sdk.version>2.33.8</aws.sdk.version>
    </properties>

    <dependencies>
        <!-- Cloud storage SPI interface -->
        <dependency>
            <groupId>org.apache.hugegraph</groupId>
            <artifactId>hg-store-common</artifactId>
        </dependency>

        <!--
          Required in docker/cloud-storage runtime: CloudStorageEventListener hooks into
          RocksDB event callback types (FlushJobInfo/TableFileCreationInfo).
          Keep this aligned with hg-store-rocksdb to avoid NoClassDefFoundError.
        -->
        <dependency>
            <groupId>org.rocksdb</groupId>
            <artifactId>rocksdbjni</artifactId>
            <version>7.7.3</version>
            <scope>runtime</scope>
        </dependency>

        <!-- AWS SDK v2 S3 client (BOM-managed subset) -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
            <version>${aws.sdk.version}</version>
        </dependency>

        <!-- URL connection client (lightweight alternative to netty for small files) -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
            <version>${aws.sdk.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>1.7.36</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Allow long-running benchmarks with sufficient heap -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!--
                      Default: skip long-running/live-service tests. They must be run explicitly, e.g.
                        -Dtest=S3PerformanceBenchmark    (benchmarks)
                        -Dtest=S3SingleLargeFileE2ETest  (needs a live S3/MinIO endpoint)
                      Explicit -Dtest=... selection overrides these excludes.
                    -->
                    <excludes>
                        <exclude>**/*Benchmark.java</exclude>
                        <exclude>**/*E2ETest.java</exclude>
                    </excludes>
                    <!-- When running explicitly, allow plenty of memory for generating 20 GB of files -->
                    <argLine>-Xmx512m</argLine>
                    <!-- Generous timeout for large data volumes (6 hours) -->
                    <forkedProcessTimeoutInSeconds>21600</forkedProcessTimeoutInSeconds>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
